package views;

import javax.swing.*;

public class OrderFrame extends JFrame {
    public OrderFrame() {
        setTitle("Đặt Hàng");
        setSize(600, 400);
        setLocationRelativeTo(null);
    }
}